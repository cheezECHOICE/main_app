enum ProductType { single, variable }

enum OrderStatus { processing, cooking, taken }

enum PaymentMethods {
  googlePay,
  phonePay,
  paytm,
  applePay,
  paypal,
  visa,
  masterCard,
  creditCard
}
